import Foundation

public enum MemoryExample {
    public static func run() throws {
        // Setup example memory service (in-memory only)
        print("Running memory service example...")
        // Add your service calls here
    }
}
